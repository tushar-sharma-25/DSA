namespace DSA_CCI.Strings;

public class IsPermutation
{
    public bool IsPermutationOfString(string str1 , string str2)
    {
        //var l = str.Length;
        string[] allPermutations = new string[] { };

        for(int i=0; i<str1.Length; i++)
        {
            for(int j=i+1; j<str2.Length; j++)
            {
                var str = str1[i];
            }
        }
    }
}